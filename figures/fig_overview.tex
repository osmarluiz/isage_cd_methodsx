% =============================================================
% Figure 1 (fig:overview) — iSAGE-CD pipeline diagram
% Layout: A on top (full width) ; B and C side-by-side below.
% Panel A label sits ABOVE the panel (image already has its own UI
% header). Panel B and C labels are inside their top-left corners,
% since they are schematic and have plenty of blank space there.
% =============================================================
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
  font=\sffamily\footnotesize,
  panel/.style={draw=black!40, rounded corners=3pt,
                inner sep=8pt},
  loopbox/.style={draw=black!50, rounded corners=2pt,
                  minimum width=2.6cm, minimum height=0.8cm,
                  fill=gray!5, align=center, inner sep=2pt},
  arr/.style={-{Latex[length=2mm]}, thick},
  panellabel/.style={font=\bfseries, inner sep=2pt,
                     fill=white, rounded corners=1pt},
  caption/.style={font=\scriptsize\itshape},
]

% ================================================================
% PANEL A — Interaction (real screenshot, full width on top)
% ================================================================
\node[panel] (A) at (0, 0) {%
  \includegraphics[width=15.4cm]{figures/screenshot_ui.png}%
};
% Label OUTSIDE (above the panel) so it does not overlap the image
\node[panellabel, above=1pt of A.north west, anchor=south west]
     {A.\ Interaction};

% ================================================================
% PANEL B — Data (annotation tree == dataset), bottom-left
% ================================================================
\node[panel, minimum width=7.4cm, minimum height=6.4cm,
      below=6mm of A.south west, anchor=north west] (B) {};
\node[panellabel, anchor=north west] at ($(B.north west) + (4pt, -4pt)$)
     {B.\ Data};

\node[font=\ttfamily\scriptsize, align=left] at (B.center) {%
Sessions/Amazon/\\
├── data/dataset/train/\\
│\ \ \ ├── images/0000.png\\
│\ \ \ └── images\_before/\\
│\ \ \ \ \ \ \ \ \ \ \ └── 0000/antes\_202108.png\\
├── iteration\_0/\\
│\ \ \ ├── annotations/0000/\\
│\ \ \ │\ \ \ \ \ \ \ └── 202108.json\\
│\ \ \ ├── predictions/\\
│\ \ \ └── models/\\
└── iteration\_1/~\dots%
};

\node[caption] at ($(B.south) + (0, 0.30)$)
     {annotation tree $=$ training dataset};

% ================================================================
% PANEL C — Training (iterative click loop), bottom-right
% ================================================================
\node[panel, minimum width=7.4cm, minimum height=6.4cm,
      below=6mm of A.south east, anchor=north east] (C) {};
\node[panellabel, anchor=north west] at ($(C.north west) + (4pt, -4pt)$)
     {C.\ Training};

\node[loopbox] (an) at ($(C.center) + (0,  2.05)$)
     {annotate\\{\tiny clicks on pair}};
\node[loopbox] (tr) at ($(C.center) + (0,  0.70)$)
     {train\\{\tiny error-weighted}};
\node[loopbox] (pr) at ($(C.center) + (0, -0.70)$)
     {predict\\{\tiny per pair}};
\node[loopbox] (re) at ($(C.center) + (0, -2.05)$)
     {review\\{\tiny model errors}};

\draw[arr] (an) -- (tr);
\draw[arr] (tr) -- (pr);
\draw[arr] (pr) -- (re);
\draw[arr] (re.west) to[out=180, in=180, looseness=2.4] (an.west);

\node[caption] at ($(C.south) + (0, 0.30)$)
     {iSAGE loop, unchanged};

\end{tikzpicture}%
}
