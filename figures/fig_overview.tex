% =============================================================
% Figure 1 (fig:overview) — iSAGE-CD pipeline diagram
% Three-panel layout: A. Interaction | B. Data | C. Training loop
% =============================================================
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
  font=\sffamily\footnotesize,
  panel/.style={draw=black!40, rounded corners=3pt,
                inner sep=10pt, minimum height=6.4cm},
  canvas/.style={draw=black, thick,
                 minimum width=2.2cm, minimum height=2.2cm,
                 fill=green!15},
  loopbox/.style={draw=black!50, rounded corners=2pt,
                  minimum width=2.5cm, minimum height=0.8cm,
                  fill=gray!5, align=center, inner sep=2pt},
  arr/.style={-{Latex[length=2mm]}, thick},
  pt/.style={circle, minimum size=5pt, inner sep=0pt,
             draw=black!70, line width=0.3pt},
  panellabel/.style={font=\bfseries},
  caption/.style={font=\scriptsize\itshape},
]

% ================================================================
% PANEL A — Interaction (real screenshot of the iSAGE-CD annotator)
% ================================================================
\node[panel, minimum width=8.8cm, minimum height=6.4cm] (A) at (0,0) {%
  \includegraphics[width=8.2cm]{figures/screenshot_ui.png}%
};
\node[panellabel, above=1pt of A.north west, anchor=south west]
     {A.\ Interaction};

% Bottom caption
\node[caption] at ($(A.south) + (0, 0.25)$)
     {clicks are co-registered on both canvases};

% ================================================================
% PANEL B — Data (annotation tree == dataset)
% Uses proper unicode box-drawing chars: ├ ─ │ └
% ================================================================
\node[panel, minimum width=5.6cm, right=8mm of A] (B) {};
\node[panellabel, above=1pt of B.north west, anchor=south west]
     {B.\ Data};

\node[font=\ttfamily\scriptsize, align=left] at (B.center) {%
Sessions/Amazon/\\
├── data/dataset/train/\\
│\ \ \ ├── images/0000.png\\
│\ \ \ └── images\_before/\\
│\ \ \ \ \ \ \ \ \ \ \ └── 0000/antes\_202108.png\\
├── iteration\_0/\\
│\ \ \ ├── annotations/0000/\\
│\ \ \ │\ \ \ \ \ \ \ └── 202108.json\\
│\ \ \ ├── predictions/\\
│\ \ \ └── models/\\
└── iteration\_1/~\dots%
};

\node[caption] at ($(B.south) + (0, 0.35)$)
     {annotation tree $=$ training dataset};

% ================================================================
% PANEL C — Training (iterative click loop, spaced out)
% ================================================================
\node[panel, minimum width=4.0cm, right=8mm of B] (C) {};
\node[panellabel, above=1pt of C.north west, anchor=south west]
     {C.\ Training};

% Boxes spaced ~1.35cm apart, filling the panel vertically
\node[loopbox] (an) at ($(C.center) + (0.30,  2.05)$)
     {annotate\\{\tiny clicks on pair}};
\node[loopbox] (tr) at ($(C.center) + (0.30,  0.70)$)
     {train\\{\tiny error-weighted}};
\node[loopbox] (pr) at ($(C.center) + (0.30, -0.70)$)
     {predict\\{\tiny per pair}};
\node[loopbox] (re) at ($(C.center) + (0.30, -2.05)$)
     {review\\{\tiny model errors}};

\draw[arr] (an) -- (tr);
\draw[arr] (tr) -- (pr);
\draw[arr] (pr) -- (re);

% Smoother loop-back arrow: wider curve around the outside-left
\draw[arr] (re.west) to[out=180, in=180, looseness=2.4] (an.west);

\node[caption] at ($(C.south) + (0, 0.35)$)
     {iSAGE loop, unchanged};

\end{tikzpicture}%
}
