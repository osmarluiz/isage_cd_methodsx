% =============================================================
% METHOD DETAILS
% =============================================================

% -------------------------------------------------------------
% 4.1 Overview
% -------------------------------------------------------------
\subsection{Overview}
\label{sec:overview}

The software shows two images of the same place at different dates
side by side (Figure~\ref{fig:overview}). The pipeline alternates
two steps, annotation and training: at each iteration the user sees
where the model improved and where it did not, and adds points
where they expect the most benefit, usually error regions. The loop
runs inside a session, a directory structure that versions the
dataset, the models, and the iterations automatically.

% ------------------------------------------------------------------
% FIGURE 1 — high-level pipeline diagram (TikZ, first pass)
% ------------------------------------------------------------------
\begin{figure}[htbp]
  \centering
  \input{figures/fig_overview}
  \caption{Overview of iSAGE-CD. (a)~The bi-temporal interface on
    the Amazon session of the validation: two synchronized
    canvases, class selection on the left, mini-map and annotation
    counters on the right. (b)~One session directory holds the
    images, the pair-anchored annotations, the models, and the
    predictions, so the annotation tree is also the training
    dataset. (c)~The iteration loop, inherited from iSAGE:
    annotate, train with an error-weighted loss, predict per pair,
    review the errors.}
  \label{fig:overview}
\end{figure}

% -------------------------------------------------------------
% Session structure
% -------------------------------------------------------------
\subsection{Session structure}
\label{sec:session_structure}

A session is a single directory that contains the images, the class
configuration, the per-iteration annotations, and the training
artefacts:

\begin{tcolorbox}[enhanced, colback=treebg, frame hidden, boxrule=0pt,
    borderline west={2pt}{0pt}{treefolder}, arc=1.5pt,
    left=10pt, right=8pt, top=8pt, bottom=8pt]
\renewcommand*\DTstyle{\ttfamily\footnotesize}
\newcommand*\treedir[1]{\textcolor{treefolder}{\bfseries #1}}
\renewcommand*\DTcomment[1]{\hfill{\color{treecomment}\rmfamily\footnotesize\itshape #1}}
\dirtree{%
.1 \treedir{Sessions/CD/}.
.2 \treedir{config/}.
.3 session\_config.json\DTcomment{current\_iteration, format}.
.3 dataset\_metadata.json\DTcomment{classes, colors, ignore\_index}.
.2 \treedir{data/dataset/train/}.
.3 \treedir{<scene>/}\DTcomment{one folder per scene}.
.4 202106.png\DTcomment{dated, co-registered images}.
.4 202108.png\DTcomment{name = YYYYMM or YYYYMMDD}.
.4 202206.png.
.2 \treedir{iteration\_0/}.
.3 \treedir{annotations/}\DTcomment{one folder per scene, JSON per pair}.
.3 \treedir{predictions/}\DTcomment{per-pair prediction PNGs}.
.3 \treedir{models/}\DTcomment{trained checkpoints}.
.3 \treedir{masks/}\DTcomment{optional dense masks}.
.2 \treedir{iteration\_1/} \ldots.
}
\end{tcolorbox}

Each scene folder is a pool of dates. The file name is the date, as
a digit string whose lexicographic order is the chronological
order, so the tool compares strings and never parses calendars.
Which two dates form a pair is not fixed at build time: any
(before, after) choice made in the interface is a valid pair, and a
scene with $n$ dates offers $n(n-1)/2$ of them.

Two configuration files sit under \texttt{config/}.
\texttt{session\_config.json} names the iteration the user is
currently annotating. \texttt{dataset\_metadata.json} declares the
classes: how many there are, which index means ``unlabeled'', and,
for each class, a name and a display colour. The interface, the
label files, and the prediction overlays all read class semantics
from this one file.

A helper script builds the session from a folder with one
subdirectory per scene, each holding two or more dated,
co-registered images, plus a classes file (a JSON list of
name and color in index order):

\begin{verbatim}
python tools/build_session.py entrada/ \
    --classes classes.json --out Sessions/CD
\end{verbatim}

The script copies the dated images into \texttt{train/<scene>/},
writes the two configuration files, and creates an empty
\texttt{iteration\_0/} with the four subfolders above. The session
is ready to open. Co-registration and resampling are upstream
concerns: the tool assumes all images of a scene share one pixel
grid.

% -------------------------------------------------------------
% 4.3 Pair-anchored label format
% -------------------------------------------------------------
\subsection{Pair-anchored label format}
\label{sec:label_format}

Every click lands in a JSON file that belongs to one pair. The path
is fully determined by the pair: a click on scene \texttt{0000}
with dates (\texttt{202106}, \texttt{202206}) at iteration
\texttt{5} goes to
\texttt{iteration\_5/annotations/0000/202106\_202206.json}:

\begin{verbatim}
{
  "format_version": "3.0-pool",
  "image": {"name": "0000/202206.png",
            "width": 1024, "height": 1024},
  "iteration": 5,
  "created_at": "2026-08-07T15:30:00Z",
  "pair": {
    "scene": "0000",
    "date_before": "202106",
    "date_after": "202206"
  },
  "annotations": [
    [268, 339, 1],
    [512, 480, 0],
    [745, 621, 1]
  ]
}
\end{verbatim}

A point is an entry of \texttt{annotations}: pixel coordinates and
a class index. Anchoring the file to the pair preserves the
semantics of a change label. The same region can be change against
one before date and no change against another, and each statement
lives in its own file, so neither contaminates the other.

The file is rewritten at every click: there is no save button and
no unsaved state. Every write is atomic: the annotator serialises
the JSON to a temporary file and only then renames it into place,
so a crash mid-write cannot leave a half-written file on disk. A pair whose
point set becomes empty has its file removed, and the scene folder
is removed with its last pair. The file tree is always a faithful
representation of the current annotation.

Each JSON is one training sample, so downstream pipelines consume
the annotation tree directly, and a leakage-free train/validation
split reduces to selecting scene folders. All pairs of a scene live
in the same folder, so no split can place two pairs of the same
scene on different sides of the partition.

% -------------------------------------------------------------
% 4.4 Bi-temporal annotation interface
% -------------------------------------------------------------
\subsection{Bi-temporal annotation interface}
\label{sec:ui}

Opening a session presents two synchronized canvases
(Figure~\ref{fig:overview}a), before on the left, after on the
right. Zoom, pan, and points are mirrored between them, and a click
on either side lands the point at the same pixel on both.

Labeling takes few gestures: a left click adds a point with the
selected class, a right click near a point removes it, a drag
fine-tunes its position, the wheel zooms, a middle-mouse drag pans.
Point colours follow the class palette declared in
\texttt{dataset\_metadata.json}.

Each canvas has a date selector. The before selector offers only
dates earlier than the current after date, so an invalid pair
cannot be selected, and changing either date switches the point set
on both canvases. Dates already annotated are marked with a bullet,
a scene opens on its most recently annotated pair, and live
counters track the session, the scene, and the pair on screen.

% -------------------------------------------------------------
% 4.5 Training in the loop
% -------------------------------------------------------------
\subsection{Training in the loop}
\label{sec:training}

The training protocol is inherited from iSAGE without change. The
user annotates points; the model is trained with an error-weighted
loss that amplifies the gradient at each labeled pixel; the trained
model produces per-pair predictions; the user reviews those
predictions, focuses on the model's confident mistakes, and adds new
points where the model is wrong. The loop repeats until the
annotations stabilize. No pseudo-labels, conditional random fields,
or foundation-model prompts are inserted between the clicks and the
loss. What iSAGE-CD adds is in the representation of the pair, not
in the training procedure.

% -------------------------------------------------------------
% 4.6 Iteration workflow and predictions
% -------------------------------------------------------------
\subsection{Iteration workflow and predictions}
\label{sec:iteration}

The pair-anchored label files described above feed the training
loop. Each round of activity is stored in a numbered
\texttt{iteration\_N/} subfolder with a small, fixed structure
(Table~\ref{tab:iteration_folder}); the
\texttt{current\_iteration} field in \texttt{session\_config.json}
names the iteration the user is currently annotating.

\begin{table}[!h]
\centering
\small
\caption{Contents of a single \texttt{iteration\_N/} folder. Each
subfolder has a single producer and a single consumer; together
they close the annotate/train/predict/review loop.}
\label{tab:iteration_folder}
\begin{tabular}{@{}lll@{}}
\toprule
Subfolder            & Produced by                        & Consumed by \\
\midrule
\texttt{annotations/} & user clicks during round $N$      & training script at end of round $N$ \\
\texttt{models/}      & training script                   & prediction script \\
\texttt{predictions/} & prediction script                 & overlay shown in round $N{+}1$ \\
\texttt{masks/}       & (optional; dense mask utilities)  & external evaluation \\
\bottomrule
\end{tabular}
\end{table}

The prediction overlay in the interface follows a simple
convention: the predictions displayed on the frame come from
\texttt{iteration\_\{N-1\}/predictions/<frame>.png}, where
\texttt{N} is the current iteration. In other words, the user
always sees the output of the model trained in the previous round
while annotating the next round --- exactly the surface the iSAGE
error-focused click strategy needs. Iteration 0 has no previous
round, so no overlay is loaded; from iteration 1 onward the
overlay is available.

Prediction PNGs use class-index values: a pixel with value
\texttt{0} is background, \texttt{1} is the first foreground class,
and so on, up to \texttt{num\_classes - 1}. This matches the class
indices declared in \texttt{dataset\_metadata.json}, so the
overlay is coloured with the same palette as the point classes:
what the user clicked and what the model predicts share a visual
language. The overlay opacity is adjustable through the control
panel; the toggle key \texttt{P} shows the overlay while held.

A new iteration is created after training completes. The training
script writes \texttt{iteration\_\{N\}/predictions/} and
\texttt{iteration\_\{N\}/models/}, creates an empty
\texttt{iteration\_\{N+1\}/annotations/} folder, and increments
\texttt{current\_iteration}. The next session re-opens on the new
iteration with the fresh predictions already visible as the
overlay --- ready for the next round of error-focused clicks.

Because the annotation tree is the dataset, sharing a round of
work between annotators reduces to sharing the annotation subtree
of an iteration. The tool provides two commands for this:

\begin{verbatim}
python anotar.py --exportar
python anotar.py --importar rotulos.zip
\end{verbatim}

The first packages the JSONs of the current iteration into a
timestamped zip under \texttt{envio/} and opens the containing
folder; the file can be sent through any channel. The second
merges the JSONs from a received zip into the corresponding
iteration folder of the local session. Pairs that were not present
locally are added; pairs that were already present are overwritten
with the incoming version, which is the simplest and least
surprising merge rule. In a typical two-person workflow one
annotator finishes a round, exports the zip, and hands it to a
reviewer, who imports it, revises the points, and either exports
back or triggers a new training round.
