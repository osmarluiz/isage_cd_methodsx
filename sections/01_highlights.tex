% =============================================================
% HIGHLIGHTS — 3 bullets, max 85 characters each.
% MethodsX requires these as a separate submission item; kept here
% inside the LaTeX draft for convenience.
% =============================================================

\noindent\textbf{Highlights}
\begin{itemize}
  \setlength{\itemsep}{2pt}
  \item Sparse point supervision for change detection, every label
        anchored to a date pair.
  \item The annotation tree is the dataset, and any trainer joins by
        a folder contract.
  \item Five rounds over 2000 Amazon tiles, 143,492 labeled pixels,
        0.007\% of the total.
\end{itemize}
